# Can we use the overide file to make compatible?
# https://www.librechat.ai/docs/remote/docker_linux#2-create-librechat-config-and-environment-files

include:
  - docker-compose.yml
services:
  # test:
  #   image: busybox
  #   env_file:
  #     - stack.env
  #   command: env

  api:
    env_file:
      - stack.env
  mongodb:
    env_file:
      - stack.env
  meilisearch:
    env_file:
      - stack.env
  vectordb:
    env_file:
      - stack.env
  rag_api:
    env_file:
      - stack.env