# Traefik TrueNAS Dockge Environment
# Copy this file to .env and modify as needed
# This is for deployment in a private network environment

# Network Configuration
# The 'proxy' network must exist on TrueNAS before starting services
# This is typically created by Dockge or your container management system

# Volume Configuration
# Using named volume 'traefik-certs' for certificate storage
# This will be created automatically by Docker Compose

# Let's Encrypt Configuration
LETSENCRYPT_EMAIL=admin@yourdomain.com
DOMAIN_SUFFIX=yourdomain.com

# Production Profile
# Use: docker-compose -f docker-compose.prod.yml up -d
